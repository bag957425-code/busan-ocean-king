# BLUE SCOUT Apps Script

Gemini API key is stored only in Apps Script Script Properties as `GEMINI_API_KEY`.
Do not commit API keys to GitHub.

The deployed web app endpoint is configured in `gemini-client.js`.
